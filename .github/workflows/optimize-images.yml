name: Optimize images

on:
  pull_request:
    branches: [ master, main, release/** ]

  push:
    branches: [ master, main, release/** ]

jobs:
  optimize-images:
    uses: Achrobat/Workflow-Repo/.github/workflows/optimize-images.yml@main
    secrets: inherit

  test-scala-presence:
    uses: Achrobat/Workflow-Repo/.github/workflows/test-scala-presence.yml@main
    secrets: inherit
